# Canon Rules (Core)
- Source of Truth = repo นี้ (JSON/Schema/Rules)
- สินทรัพย์หนัก (SVG/PNG/Models) = `canon-assets`
- ทุกโลกต้องผ่าน `schemas/world.schema.json`
- ชื่อไฟล์ snake_case, ASCII
- เปลี่ยน Canon = ผ่าน PR + CI เท่านั้น